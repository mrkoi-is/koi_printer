## 0.2.0

* **Feature**: Added "Layers" (图层面板) tab to `LeftPalette` with drag-to-reorder support using `ReorderableListView`.
* **Testing**: Added UI Widget Tests for `LeftPalette` and element addition workflows.
* **Coverage**: achieved 98%+ test coverage for state, commands, and mock data templates.
* **Architecture**: Refactored `EditorState` into cohesive mixins (`EditorElementMixin`, `EditorSchemaMixin`, etc.).

## 0.1.0

* 初始发布 (Initial release)
